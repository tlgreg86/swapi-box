import React from 'react';
import { shallow, mount } from 'enzyme';
import Button from './Button';

describe('Button tests', () => {
  // it('should render a button')
  
  // it('should default to Favorites being active')
  
  // it('should set People to active on click')
    // ...maybe repeat this test for the other tabs
  
  // it('should start with 0 favorites')
  
  // it('should display the correct Favorites count')
  
  // it('should call handleClick on click')
});